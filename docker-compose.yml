version: '3'

services:
  app:
    build: ./
    networks:
      - rede
    ports:
      - "8080:5000"

  mysql:      
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: prod
      MYSQL_USER: root
      MYSQL_PASSWORD: 1234
    networks:
      - rede
    ports:
      - "2020:3306"

networks:
  rede:
    driver: bridge
